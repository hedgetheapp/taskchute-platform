# Copy to .dev.vars and replace both secret values. Never commit .dev.vars.
# Keep disabled normally. Set exactly "true" only while intentionally bootstrapping.
BOOTSTRAP_ENABLED=false
BETTER_AUTH_SECRET=
BOOTSTRAP_TOKEN=
