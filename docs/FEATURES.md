# Features

この文書のStatusはFeature development statusを示す。

Verificationの正本は`docs/TEST_MATRIX.md`であり、`Implemented`等のFeature statusだけを理由に`Verified`と判断しない。

Status values: Planned / In design / Implemented / Verified / Withdrawn (historical compatibility)

| Feature | Status | Notes |
|---|---|---|
| Project management | Implemented | D-065 Approved。dedicated Settings Project Boardでcreate、inline rename、active / archived tabs、search、archive / restore、whole-row reorder target、row-end menu、centered hard-delete confirmationを実装。server-owned board order / revision、active-only new assignment、current archived assignmentの理解可能な表示、Project hard delete時のTask `project_id=NULL`、Routine / Entry / Execution / operation history保持、bounded historical Project snapshotsを実装。APP `0019_project_management.sql`、Decision `09934a48e9d39126f4b377d9376977a5a45270d9`、implementation `af5a3bd46e3f490e23fafe62c24d54f119584f3c`。corrective `45d398bb282e6d892e63a31706722783c7693b31`で全Project commandのcanonical reconcile、未収束ambiguous exact retry、Delete origin / fallback focus restoreを統合し、backdrop focus fix `3208d1edde6b1db4ebd4272cf5c4e16d00a385e0`を追加。Worker `184 / 184`、Web `185 / 185`、migration regression `4 scenarios`、typecheck / build / exact nonprod dry-runはPASS。persistent nonprod final verificationは2026-09-06にPASSし、fresh APP / AUTH backup・isolated recovery、active selector order、Task-assigned disposable Project hard delete、Task `project_id=NULL`、Day / reload persistence、next-row focus fallback、APP / AUTH integrity、console空集合を確認。production NOT_RUN、Released NO |
| Day Table modal / row Tab / non-blocking mutation workflow | Implemented | D-063 Approved。共通centered Modal（dialog semantics、focus trap、Escape / backdrop / X、focus restore）、visual-order row Tab（Bulk / Execution除外）、focused Taskの`X` selection、single-cell actual commit boundary、scope-based concurrent saves、pending overlay / multi-status、ambiguous exact retry registry、Complete→Start queueを実装。既存API / Domain / schema / migration / dependency / security postureは不変。Decision `dfaad7c0ff1fc9687025a1e3f28399d139552cca`、implementation `79c2a7cff88b36886f275f635ff9cccdd5d58f83`、Web `171 / 171`、Worker `180 / 180`、typecheck / build / nonprod safetyはPASS。authenticated browserはHelp / `0900` / End非編集 / EscapeをPASS、残りのremote feature workflowはNOT_VERIFIED、production NOT_RUN、Released NO |
| Day Table keyboard workflow / single-cell actual editing / Task-column resize / calendar polish | Implemented | D-062 Approved。neutral Day surfaceからの`↓ / J / ↑ / K` navigation、`S / N / E / D / ? / Esc` shortcut、input / IME suppression、4桁`HHMM` actual single-cell editor、Task列280〜640pxのbrowser-local resize persistence、month / year calendar controls、outside click close、`--:-- / --分` placeholderを実装。D-057 / D-060 semanticsを再利用し、API / Domain / schema / migration変更なし。implementation `327dd7f`、neutral main focus fix `0344e65`、local `164 / 164` Web / `180 / 180` Worker、real-local safety、persistent nonprod deploy / authenticated browser evidenceはPASS、production NOT_RUN、Released NO |
| Server canonical task state | Implemented | current TaskChuteDay / Project / Task / Entry / Execution authorityをServerへ実装。First Server + Web vertical sliceをPR #3 + #5で統合 |
| Day Table editor ergonomics and resize anchoring | Implemented | D-061 Approved。planned / actual Start・Endを4桁`HHMM`のinline text inputへ統一し、Day Table Estimateを分表示、cell幅入力、blur / Enter / Tab / Shift+Tab commit、Escape cancel、Start→End focus間の早期commit防止、左端固定のcolumn resize、destructive delete styleを実装。既存D-043 / D-057 / D-060のmapping・lifecycle・overlap・retry semanticsは維持し、SQLite millisecond-safe execution guardを修正。implementation `e4cea330f0e369ef5449d8596dd8bc0d40360df8`、follow-up `1c07467173e17e232d46cdd704a3c200409b665e` / `7b435ec3d31ccbd627d5adb46d86defeece16955`、local / persistent nonprod deploy・authenticated browser・D1 integrityはPASS。migration / schema / API / Domain変更なし、production NOT_RUN、Released NO |
| Day row editing / inline actual entry / completed duplicate | Implemented | D-060 Approved。current-Day ordinary planned EntryのTask名inline編集、owner-scoped Project選択、旧dialogなしのStart / End direct inline編集を実装。actual editorの表示形式はD-061で4桁`HHMM`へ更新した。D-057のSetExecutionTimes semantics / overlap / retry / forecast reconciliationを再利用し、RevertEntryStartは復活させていない。completed current-Day Entryは`… → 複製`のみ許可し、delete / date moveは不可。D-069でestablished future Dayのordinary planned EntryへProject assignmentだけを拡張し、future Task名はread-onlyとした。APP `0017_task_metadata_update.sql`はoperations command allow-listのみを追加し、Task / Entry schemaは不変。implementation `18c2d6a1f41f9eeff7eba56ecb2ba3781d13658e`、D-069 `bbcdcbdea14564f688065207462489fbdb349392`、local automated / persistent nonprod deploy・safetyはPASS、production NOT_RUN、Released NO |
| Core Domain foundations | In design | D-015 Approved。Task / Entry / Project / Section / TaskChuteDay / Execution / first lifecycleは実装。Routine / richer history / advanced Day planningは設計中 |
| Task / Entry identity | Implemented | separate stable UUIDv7 identityをruntimeへ実装。ID timestampはordering authorityにしない |
| TaskChuteDay | Implemented | explicit timezone/boundary bootstrap、materialized interval/context、compatible DST semanticsをruntimeへ実装 |
| Day Navigation v0.1 | Implemented | visual interaction targetは`docs/DESIGN.md`、semanticsはD-041 / D-042でcanonical化済み。commit `6f183c28242a1cd30211ea8e0c4f1e1dc23329bb`で未来日non-materializing preview / first successful mutation atomic establishment、custom calendar / Today / keyboard navigation、past unestablished record-none / read-only / no-backfill、established past frozen history表示、auth境界でのselected Day resetを実装し、evidence docs commit `be52305ed98e2dbd213b99dcdddb34602cf69091`までGitHub `main`へIntegrated。R-016 follow-up correction commit `04254f60b1dfb25e66550b940b9df6b28fdf616f`で、established future Dayへの後続Addをcanonical Day / current placement revision / frozen contextへ収束させた。source review / local automated / signed-in general browser / R-016 real-local browser / persistent nonprod representative verificationはPASS。remote logout→relogin、cross-owner等の未実施subcase、productionはNOT_RUN、Released NO。future Routine preview、past historical correction、non-current Day executionは含まない |
| Day Operations v0.1 | Implemented | D-056 Approved。single / bulk date moveとsingle planned deleteのruntimeを実装し、past established sourceからcurrent / future、month / year boundary、Section維持＋target Day frozen start再同期、Routine origin保持・moved occurrence protectionを扱う。implementation `b325580d720099e93a9036d5091b267889e4753b`、APP compatibility migration `0015_day_move.sql`、corrective `72321c7b51a7aec2e1123d216262e9dd8b88d497`を`main`へIntegrated。local / real-local / persistent nonprod migration・deploy・safety integrityはPASS。moved Routineのper-affected-Day frozen Section start corrective regressionはPASS、schema / migration追加なし。remote authenticated feature mutation / reload / exact replayはbrowser connector境界によりNOT_VERIFIED、production NOT_RUN、Released NO |
| Project | Implemented | CreateProject + optional Task project relationを実装。Day TableのProject列targetは`docs/DESIGN.md`でcanonical化。Project dropdown quick create、検索、設定画面でのuser-defined orderはbroader UI scopeとして設計継続 |
| Section | In design | user-global stable persistenceとD-038 B1 foundationに加え、B3のrename / boundary edit / add / delete・absorption、immutable configuration version/head lifecycle、next-Day effective Web settingsをcommit `2481c4916ca2f694f07d6808a4482bea28c79a80`で実装。B3 source / automated / real local migration / browser / persistent nonprodはPASS、remote raw console exact countはNOT_VERIFIED、productionはNOT_RUN、Released NO。icon / accent、summary、Day-boundary lifecycle等のbroader scopeは未実装 |
| Section Day summary | In design | `docs/DESIGN.md`でcontinuous table上のSection summary row、完了/総数、合計見積、空Section、Sectionなしgroup、日別collapse、Section直下Task追加をtargetとしてcanonical化。UI-2B commit `3861b9839b55a1453b0e2f230f03728e8d85059b`でnormal Section / `Sectionなし`のcollapse / expandとlogical Day別in-session stateをmainへIntegrated。Section collapse persistence v0.1はcommit `b81ea533c27dbcf81e3baae865f361d0f40f66e3`でbrowser-localのlogical Day + stable Section identityへ拡張し、local / persistent nonprod verificationはPASS。Server同期、cross-device preference、Execution実績によるcapacity barを含むbroader projectionは設計継続 |
| Section collapse preference | Implemented | `b81ea533c27dbcf81e3baae865f361d0f40f66e3`でversioned browser-local preferenceを実装。normal Sectionはstable Section ID、`Sectionなし`は`unsectioned` sentinel、logical Day別にreload復元し、malformed / incompatible / stale dataは安全に扱う。API / Worker / D1 / migration変更なし。local automated / signed-in nonprod browser / APP integrityはPASS、production NOT_RUN、Released NO |
| Desktop Day wide layout + Sidebar collapse | Implemented | `74f7b24`（`Widen Desktop Day layout`）でDay-only `.shell` wide override、`minmax(280px, 1fr)` Task track、authenticated Sidebar open / closed、closed時のmain grid track解放、versioned browser-local preference `taskchute.web.sidebar.v1`（`{version:1,open:boolean}`）を実装。Settings / Routine Board / Authのcommon `.shell` cap、既存table-owned horizontal scroll / narrow sticky fallbackは維持。local focused `120 / 120`、full Web `126 / 126`、typecheck / build / diff-check、persistent nonprod browser / API safety / APP-AUTH integrityはPASS。Sidebar resize / saved width、productionはNOT_RUN / 未実装 |
| Mode | Implemented | D-026 / D-068 / D-070 / D-071 / D-072 Approved。1 Entryに0..1 Mode、Mode Boardのowner-scoped persistence / rename / reorder、Day TableのMode column、planned live title / Start snapshot、established future Dayのset / replace / clearを実装済み。D-071はMode Settings Boardのshared visual / interactionをProject Board conventionへ揃え、D-072でcurrent-tab title search、active / archived tabs、reversible archive / restore、確認付きlive hard delete、active-only assignment、historical snapshot保持を追加した。D-072 UI correctiveでMode専用のright-align overrideを削除し、Projectと検索 → 使用中 / アーカイブ → `?`のtoolbar左揃え・間隔・順序をpersistent nonprod browserで実測一致させた。local / persistent nonprod migration・deploy・authenticated browser E2E・DB integrityは2026-09-07にPASS。production NOT_RUN、Released NO。Routine default / override、bulk変更は未実装 |
| Project / Mode selector search | In design | Project / Mode selectorはdirect文字一致、正規化、かな/カタカナ統一、romaji→かな検索を扱い、例として`tasuku`から`タスクシュート開発`を候補化する方向。漢字読みindexのexact方式は実装設計で詰める |
| Entry planning metadata | In design | D-026 + D-031 + D-039 + D-043 Approved。B1のEntry見積とB2 planned startはmainへIntegrated済み。D-043のreal Section / range内non-null開始予定と`Sectionなし` / `NULL`のfull synchronizationはimplementation commit `7d3c0cb0881dfc11725af6ff45eabad69f86a22a`でordinary EntryとR2A選択scopeへ実装し、evidence docs commit `d1283eb6ef10c9a0997a36427a09b89042250f96`までGitHub `main`へIntegrated済み。D-054 corrective fix commit `c0eed8452c340a2798fc6d2e62532cc7affd1c4f`でfuture propagationのtarget planned startをaffected Dayごとのfrozen Section contextへ修正し、differing-context / start-only regressionをlocalでPASSした。source review / local automated / real-local migration・browser / persistent nonprod representative verificationはPASS、今回のcorrective fixのschema / migration変更はNOT_REQUIRED、productionはNOT_RUN、Released NO。broader planning metadata scopeは設計継続 |
| Planned-start ordering | Implemented | D-031 + D-039のderived canonical order、manual tie-break、atomicity / retry、Reorder cohort制約はmainへIntegrated済み。D-043のexplicit real Section選択によるSection開始minute設定とplanned-start clearによる`Sectionなし`同期もcommit `7d3c0cb0881dfc11725af6ff45eabad69f86a22a`でmainへIntegrated。source review / local automated / real-local browser / persistent nonprod representative verificationはPASS、remote detailed retry / concurrency等はNOT_RUN、productionはNOT_RUN、Released NO |
| Start forecast | In design | D-032 Approved。開始予定とは独立したprojection。v0.1はcommit `8939c4d6af95e2fd21b7d91e0e946bee29a6c1fb`でImplemented / Integrated、代表persistent nonprod verificationもPASS。current progress / Day order / estimateを使いSectionをまたいで連続計算し、past / completed / running自身 / Sectionなしは表示対象外。broader scopeは設計継続 |
| Today / DayBoard ordering | Implemented | explicit Entry order + placement revision conflict protectionに加え、D-031 / D-039のNULL-first、planned minute昇順、same-minute manual tie-break、cohort-aware ReorderをB2で実装。local + persistent nonprod verification PASS |
| Day Table interaction | In design | UI-1 / UI-2A / UI-2B / UI-2C / cross-Section D&D v0.1、Start Forecast v0.1、Day Table column customization v0.1、Bulk Selection v0.1 / v0.2A / v0.2B1 / v0.2B2、D-058 simplification、D-059 viewport polish、D-060 row editingをmainへIntegrated。default named orderは`実行 / Task / Project / Mode / Section / Routine / 見積 / 開始予定 / 開始見込 / 開始 / 終了 / 実績`で、table-owned horizontal scroll、reserved Bulk slot、実用幅でのBulk / Execution / Task fixed-left foundation、Section collapse、browser-local Day/Section/column preference、focused Taskの`S`、same-Section / same-cohort D&D、ordinary planned Entryのcross-Section append D&D、D-032 read-only forecast、Execution factsからのread-only actual projection、D-051のBulk Selection、far-right planned-row overflow menu、D-060のordinary planned Task metadata editor / Project selector / direct actual editor / completed duplicateを実装済み。D-059ではTask rowのheight / densityを変更せず、Day Table surfaceをviewport残余へ拡張し、stable scrollbar gutter、square / centered Bulk checkbox、eligible planned rowのnon-interactive full-row D&D（interactive descendant除外）を実装した。D-060でSetExecutionTimesだけをcurrent capabilityへ再有効化し、RevertEntryStartはcurrent capabilityから撤去したまま0016と既存historical dataだけを保持する。local automated、persistent nonprod migration / safety、API safety、APP/AUTH integrityはPASS、authenticated remote save / reload / completed duplicateはNOT_VERIFIED、production feature verificationはNOT_RUN、Released NO。Mode、Note、Search/Filter/full context interaction、Routine-derived等のcross-Section D&D writeは引き続き未実装 |
| Day Table column customization + actual projection | Implemented | implementation commit `2bf40a0`と既存fix commitsでregistry-driven Project以後のheading / row / draft alignment、header reorder、shared resize、double-click auto-fit、Columns menu hide/show、browser-local preference `taskchute.web.day-columns.v2`（`{version:2,order:[...],widths:{...},hidden:[...]}`）、valid v1 migration、Routine compact icon presentation、Execution factsからのderived read-only 開始 / 終了 / 実績 projectionを実装。D-058でcurrent Start Revertを撤去し、D-060でSetExecutionTimesだけをinline actual editorとして再有効化した。hide/showはfull order / widthを保持し、`すべて表示`はorder / widthを保持、resetはcolumn presentationだけをdefaultへ戻す。fixed Bulk / Execution / Task、Mode / Noteは対象外。D-060 Web `157 / 157`、Worker `180 / 180`、migration regression `4 scenarios`、typecheck / buildはPASS。authenticated remote save / reloadはNOT_VERIFIED、production NOT_RUN、Released NO |
| Day Table cross-Section D&D v0.1 | Implemented | commit `89d4784fddca891421d3619def352ee1156f1c89`でordinary planned Entryを同一Dayの別visible Section summary（`Sectionなし`含む）へappendするfirst sliceを実装。expanded placeholder、collapsed cue / no auto-expand、floating dragged row、既存`MoveEntry` 1回、full-Day reconciliation、D-043 planned-start synchronization、same-Section Reorder / `Shift+↑/↓`維持、visible `↑/↓` buttons撤去を含む。Routine-derived、running / completed、read-only / preview / lockedはno-write。focused `116 / 116`、full Web `122 / 122`、Worker / D1 `147 / 147`、typecheck / build / diff-check、persistent nonprod deploy / authenticated browser / API safety / APP integrityはPASS、production NOT_RUN、Released NO |
| Day task move / duplicate / delete | In design | D-037 Approved。未実行Taskの日付移動、fresh duplicate、single-entry delete、completed / interrupted historical row保護を設計。D-050のDuplicate first sliceとD-051のBulk Selection v0.1は別rowでImplemented / Integratedし、cross-Day move、single-entry delete、completed / running source等のbroader scopeは設計継続。running deleteはcancelled historical representationを残す |
| Day task duplicate first slice | Implemented | D-050 Approved。implementation commits `1d68a74148da211bfae76b6f36b86cb18f23e7fc` / `47d998e37bd12fc591b43c3624324ad237f3ca46` / `3573aaafcfcda651bc850dae706f8dd5157efe65`で`main`へIntegrated。current / established futureのplanned source、`Sectionなし`、Routine-derived sourceからnormal planned duplicateを作成し、identity / field copy / immediate-below order / retry boundary / lifecycleを維持。local automated、real-local、persistent nonprod migration / browser / integrityはPASS、productionはNOT_RUN、Released NO。cross-Day / protected source / Mode / Note / single-entry deleteは未実装 |
| Start / Complete lifecycle | Implemented | current runtimeはplanned -> running -> completed、retry safety、active Execution max 1、no implicit interruptを実装。D-058ではread-only actual projectionとnormal lifecycleを維持し、D-057 correction commandはcurrent capabilityから撤去 |
| Manual Execution correction | Withdrawn (historical compatibility) | D-033 / D-057のDecision、実装、0016 migration、既存Execution / operation dataはhistorical recordとして保持するが、current Web / client API / Worker command pathから撤去。開始 / 終了 / 実績のderived read-only projection、no-overlap / valid historical fact protectionは維持。D-058 implementation `66d63efa790c06d2efefa769595508b7c5d6dbb5`、negative API boundary、persistent nonprod safetyはPASS |
| Interrupt / continuation | In design | D-028 Approved target。explicit Interruptでcurrent Executionをinterrupted終了し、割り込みTaskをStart、元Task continuationを直下生成。current runtimeは未実装で通常Startをreject |
| Quick Interrupt | In design | D-028 Approved。実行中Taskから`（割込）`を即生成・Startし履歴へ残す。発生時刻のcurrent Sectionへ配置し、nested Quick Interruptも許容。exact persistence / command contractは未決 |
| Revert current Start | Withdrawn (historical compatibility) | D-029 / D-057の狭いcurrent Start Revert semantics、implementation、0016 migration、既存operation / Execution dataはhistorical recordとして保持するが、current UI / client API / Worker command pathから撤去。D-058はnormal Start / Completeとread-only actual projectionを維持し、旧API pathは404。authenticated remote feature mutationはNOT_VERIFIED |
| Floating Runner | In design | current running TaskをMain content下部付近のcompact floating UIで継続表示。whole logical work chain progress、subtle estimate overrun、Quick Interrupt / revert / Complete、minimizeはhistorical design referenceに残るbroader UI scopeで、current `docs/DESIGN.md`では未canonical化 |
| Next Entry projection | Implemented | explicit order上のplanned Entryからlifecycle-aware Nextを算出。Next以外のplanned EntryもStart可能 |
| Historical fact foundation | In design | TaskChuteDay interval + Execution factは実装。D-028〜D-033 / D-037でinterrupt、Start取消、Section historical semantics、manual time correction、running delete時のnon-destructive cancellationをApproved。D-038でSection version/config/day-context責務分離をApproved。Section以外のsnapshot / cancelled outcome等は未決 |
| Day planning persistence B1 | Implemented | PR #13でmainへmerge・Integrated済み。local automated + source review + real local APP migration + signed-in browser + persistent nonprod migration/runtime/browser verificationはPASS。production verificationはNOT_RUN、Released NO |
| Day planning persistence B2 | Implemented | `0004_dogfood_day_b2.sql`、planned-start persistence / command、derived placement/order、Web editorをcommit `316ad0d88f0f88d1445991904da587b1e0987dab`でmainへIntegrated。source review、Worker 87、Web 49、isolated migration 25 checks、real local migration / browser、persistent nonprod migration / runtime / authenticated browserはPASS。productionはNOT_RUN、Released NO |
| Day planning persistence B3 | Implemented | `0005_dogfood_day_b3.sql`、Section settings query/update、immutable version append / expected-head switch、current-Day freeze / next-Day effective semantics、Web settings panelをcommit `2481c4916ca2f694f07d6808a4482bea28c79a80`でmainへIntegrated。source review、Worker 91、focused B3 3、Web 55、migration 32 checks、real local migration / browser、persistent nonprod migration / preservation / deploy / authenticated browserはPASS。next-Day real browserはNOT_RUN、remote raw console exact countはNOT_VERIFIED、productionはNOT_RUN、Released NO |
| First runtime bootstrap slice | Implemented | auth、current TaskChuteDay、CreateProject、AddTaskToDay、DayBoard / reload recoveryをPR #3でmerge |
| First Server + Web vertical slice | Implemented | PR #3 + #5でD-013 scopeを実装・main統合。local + persistent nonprod evidenceはcurrent TEST_MATRIXを参照 |
| Web app | In design | React + Vite SPAとFirst vertical slice UIは実装。broader Product UI / responsive / routing / offline等は設計継続 |
| Settings navigation v0.1 | Implemented | commit `51242b08e015817108010839cd5234959da2fed5`でDesktop Left Navigationの`今日` / `設定`、Settingsの`Section` / `Project`、owner-scoped Project list / createを実装し、DayBoardのtemporary Section settings / standalone Project createを撤去してmainへIntegrated。Section draft navigation保持 / explicit Cancel、Sidebar初期幅240px、UI-1 7列 / 独立Routine列を維持。source review、automated、signed-in local browser、persistent nonprod authenticated browser / integrityはPASS。production `NOT_RUN`、Released `NO`。broader Project管理、Mode Settings、Sidebar resize / saved custom widthは未実装。Sidebar open / closed preferenceはDesktop Day wide layout rowを参照 |
| Web First vertical slice | Implemented | Project / Task+Entry / reorder / Start / Complete / Next / retry-conflict reconciliationをasync UIで実装 |
| Async Web mutation | Implemented | First vertical sliceのProject / Task+Entry / Reorder / Start / Completeをfull-page reloadなしで実行 |
| Web browser reload recovery | Implemented | Server canonical stateから再取得・復元。ambiguous mutationもcanonical refetchへreconcile |
| Cloudflare Workers API | Implemented | auth / bootstrap / current day / Project / Task+Entry / Reorder / Start / Complete APIを実装 |
| Cloudflare D1 application persistence | Implemented | AUTH_DB / APP_DB migrations、owner-scoped persistence、operations、executions、active Execution constraintを実装 |
| D1 concurrency / atomicity spike | Verified | D1-SPIKE-01〜08 current-harness local + temporary remote PASS |
| Application authentication | Implemented | Better Auth 1.7.1、public signup disabled、operator bootstrap、rolling 7日session、separate AUTH_DB / APP_DBを実装 |
| Android app | Planned | native first-class。Kotlin + Jetpack Compose第一候補 |
| Android offline capability | In design | capability自体はApproved。操作範囲 / sync方式は未決 |
| Android Widget | Planned | Android architectureを再利用 |
| Wear OS / Pixel Watch | Planned | companion target。Compose for Wear OS第一候補、exact scope未決 |
| iOS native app | Planned | Future / low priority。Swift + SwiftUI第一候補。まずWeb clientから利用 |
| Notes/Documents | In design | Markdown-native + shared Document foundation。standalone Document / general noteをfirst-class capabilityとして扱い、Task / Project等のDocumentと共通foundationを利用 |
| Standalone Document / general note | In design | Task / Project等に従属しない独立Noteを作成可能とするApproved direction。exact lifecycle / organization / search UXは未決 |
| Document links / backlinks | In design | Document同士をlinkし、backlinkから逆方向に辿れるcapability。exact syntax / rename semantics / indexing / Graph Viewは未決 |
| Project Primary Document | In design | logical 1 Primary Document。physical lazy creation可 |
| Task Primary Document | In design | logical 1 Primary Document。Routine共通の長期noteにも利用 |
| RoutineOccurrence Document | In design | optional日別Document。interrupt continuationで複数Entryになっても同じOccurrence文脈を共有可能 |
| Day-specific task context | In design | D-027 / D-034 Approved。Routine occurrenceではTask名 / Project / Mode / Section / 見積 / 開始予定 / day-specific Note等を項目単位override可能。past contextはretroactiveに変更しない |
| Task Note pane | In design | Day Tableの独立Note列から、能動的に開いたTaskへ固定したright paneを表示。Task名 + Note editorを中心にし、J/K row focusへ自動追従しない |
| Markdown comments | Planned | images対応を含む |
| Image attachments | In design | shared Attachment capability。binary storageは未決 |
| Routine foundation | Implemented | D-040 / D-043〜D-048 Approved。R1 / R2A / R2BはIntegrated。R2B commit `304e73f`でTask 0..1 Routine、Routine Board、typed recurrence / pause / suppression / snapshot、independent Board orderを実装。source review / local automated / real-local `0008` migration・browser / persistent nonprod migration・deploy・authenticated representative browserはPASS。controlled inclusive end-date browser subcaseは`TOOLING_BLOCKED / NOT_VERIFIED`、historical title / Projectのpast-Day browser subcaseとproduction deep mutationはNOT_RUN。D-049 initial production smokeはPASS、Released YES |
| Routine Board R2B | Implemented | D-047 / D-048をcommit `304e73f`でmainへIntegrated。Sidebar Board、OFF create、daily / N-day / weekly、inclusive period、pause/resume、schedule suppression/restore、Task metadata snapshot、inline defaults、independent reorderを実装。D-064でcurrent Boardを`有効 \| タスク名 \| 繰り返し \| 開始予定 \| 見積 \| プロジェクト \| セクション \| 開始日 \| 終了日`へ整理し、header reorder / resize、browser-local preference、Routine archive delete、J/K/上下、Helpを追加した。OFF metadata更新による誤suppressionはsource review / real-localで検出・修正済み。local / real-local / persistent nonprod migration / deploy / browser evidenceはPASS。productionはnavigation / empty Board / reload smokeのみPASSで、deep mutationはNOT_RUN |
| Minimal Routine R1 | Implemented | D-040 Approved。runtime commit `f9324e866deb74277d2fd83c5945f2df4b2b95da`をPR #14でmainへIntegrated。R1 baselineのRoutine planning field read-only境界は、integrated R2A commit `7d3c0cb0881dfc11725af6ff45eabad69f86a22a`のdedicated scope commands / UXでcurrent-Day planned Entryに限り拡張済み。R1 local / persistent nonprod evidenceはPASS。R2Aもsource review / local automated / real-local migration・browser / persistent nonprod migration・deploy・authenticated browser PASS、remote multi-Day propagationはNOT_RUN、production NOT_RUN、Released NO |
| Routine defaults / day override | Implemented | D-034 + D-044 + D-045 + D-046 Approved。implementation commit `7d3c0cb0881dfc11725af6ff45eabad69f86a22a`でcurrent-Day planned Routine-derived Entryの`Section + 開始予定`同期unitと独立見積unit、no-write candidate、unit別`今回だけ / ルーティンに反映`、typed occurrence override、current defaultへのreset、eligible propagation、Routine default revision、D-045 normalizationを実装し、evidence docs commit `d1283eb6ef10c9a0997a36427a09b89042250f96`までGitHub `main`へIntegrated済み。Source Review、Worker / D1 `117 / 117`、Web `78 / 78`、migration `2 scenarios`、typecheck / build / diff-check、real-local `0007` migration・preservation / signed-in browser、persistent nonprod `0007` migration・preservation / exact-main deploy / authenticated representative browserはPASS。remote multi-Day propagation / detailed retry / concurrency等はNOT_RUN、productionはNOT_RUN、Released NO |
| Routine recurrence | In design | D-047 R2B candidateで毎日 / N日ごと / 曜日指定とinclusive periodをlocal実装。営業日、休日、祝日、N-week、monthly variantsはD-035 / D-036のbroader designで未実装 |
| Workday / holiday calendar | In design | D-035 Approved。日本の土日祝base + user指定休日 + 営業日扱いoverride。各overrideに任意の自由入力理由を持てる |
| Routine stop / resume / delete | Implemented (delete) | D-034 / D-064 Approved。停止期間はbackfillせず、Routine deleteはowner-scoped archiveによってBoardから除去しfuture generationを停止するsoft-delete。RoutineDefinition、Task、Occurrence、Entry、Execution、history、snapshot、moved occurrenceは破壊しない。D-064 APP `0018_routine_archive.sql`、local / real-local、persistent nonprod migration / deploy / authenticated disposable delete / reload verificationはPASS。Routine stop / resumeのbroader lifecycle UIは引き続き設計継続 |
| Routine generation / materialization | In design | D-034 Approved。必要範囲をprojectし、day-specific edit / move / Skip / execution等でmaterialize。exact persistence / query cachingは未決 |
| Routine streak / achievement | Planned | exact achievement / delayed completion / continuation semanticsは未決 |
| Review | Planned | historical factsからのprojection。D-028〜D-033 / D-037のvalid actual / correction / cancellation semanticsに従う |
| Keyboard-first Day interaction | In design | `docs/DESIGN.md`で`J/K`・上下、`Shift+↑/↓`、`S`、Tab traversal、IME safety、Section summary focusをDay Table targetとしてcanonical化。`U` / `I` / `D` / `X` / `F` / `Ctrl+C` / `Shift+F10` / Day移動等のbroader mappingは未canonical化 |
| Hit-a-Hint | In design | `F`でvisible actionable elementへhintを表示。mode中は通常single-key shortcutを停止し、入力はhint sequence専用 |
| Bulk Selection | Implemented | D-051 Approved。reserved Bulk slotのrow checkbox / Day projection全体へのselect-all、ephemeral stable Entry ID selection、ordinary EntryのDay-only remove、Routine-derived Entryのone-day `skip`、explicit confirmation、atomic owner / revision guard、operation replay / rollback、reload / navigation / auth境界のselection clearを実装。implementation commits `c0c8799f1ae5fd1b14fd5f87259adda02ffca504` / `ae7cd5c980f202bad5ca36e2967a010d8457cc1e`でmainへIntegrated。local automated、real-local、persistent nonprod migration / deploy / authenticated browser、mixed ordinary delete + Routine skip、reload persistence、Task / Routine identity preservation、placement revision `+1`、operation log、same-operation retry、D1 integrity evidenceはPASS。Bulk Section v0.2Aは下行のD-052 scopeでIntegrated。production、undo / restore、running cancellation、completed / interrupted delete、persisted selection、Bulk date / Project / Mode変更は未実施・scope外 |
| Bulk Section change v0.2A | Implemented | D-052 Approved。ordinary planned Entryだけを一つの`BulkMoveEntriesToSection` commandで同一Day内のSectionへ変更し、D-043のreal Section / `planned_start_minute`同期、Sectionなしの`NULL`同期、stable display-order append、same-Section start-only sync、canonical no-op、atomic owner / revision / snapshot guard、operation replay / rollbackを実装。Routine-derived / running / completed / historical / preview / locked selectionはno-write / rejectとする。implementation commit `1a21c313d131d0a8e3673a9c4b68bce3282c30ff`、APP migration `0011_bulk_section_change.sql`、local / real-local / persistent nonprod migration / deploy / authenticated browser / D1 integrity evidenceはPASS。migration前full DB backupは未取得、post-migration private recovery backupは取得・readability検証済み。Bulk Routine / date / Project / Mode / Note変更、cross-Day、undo / restore、production deep mutationは未実施・scope外 |
| Bulk Section change v0.2B1 | Implemented | D-053 Approved。D-052のcurrent-Day Section変更をRoutine-derived planned Entryへ拡張し、ordinary / Routine / mixed selectionを一つの`BulkMoveEntriesToSectionOccurrence` commandで処理する。Routineを含むselectionは明示的な`今回だけ変更` acknowledgementを要求し、R2A typed occurrence overrideをcreate / replaceする。real Sectionはserver-derived `section_id + planned_start_minute`、Sectionなしはordinary / Routineとも`NULL + NULL`、同じeffective pairでno overrideならoverride-only persist、同じexisting overrideならtrue no-op、visible change時だけrevision exactly `+1`、mixed display-order stable append、atomic guard / replay / rollbackを維持する。implementation commit `70d88e2be8d33ada2e520029f2b322301d30c5d0`、APP migration `0012_bulk_routine_section_occurrence.sql`、local / real-local / persistent nonprod migration / deploy / authenticated browser / D1 integrity evidenceはPASS。RoutineDefinition default / defaults_revision、future / other Occurrence、schedule / Board / suppressionは変更しない。Bulk `Routineへ反映`、future / past Routine bulk、date / Project / Mode / Note、cross-Day、undo / restore、production deep mutationは未実施・scope外 |
| Bulk Section change v0.2B2 | Implemented | D-054 Approved。selected Routineごとにpreselectなしで`今回だけ` / `ルーティンに反映`を選択し、`すべて今回だけ` / `すべてルーティンに反映`のfill-all後も個別overrideできる一つの`BulkMoveEntriesToSectionScoped` commandを実装。ordinaryはcurrent-Day only、OccurrenceはD-053、DefinitionはD-044のdefault update / selected override clear / eligible materialized propagationを再利用し、unique Definitionのdefaults revisionを一回、affected Dayのplacement revisionを一回だけ増分する。migration `0013_bulk_routine_section_scoped.sql`はoperations CHECK-onlyで、既存schema / rows / identitiesを保持。implementation `ddbf882e2ec9487175ccdc96e5d27184d8a1b5b2`、local / real-local / persistent nonprod migration・backup gate・deploy・authenticated browser / D1 integrityはPASS。remote materialized-future propagationとexact remote replay probeはfixture / browser tooling境界により`NOT_VERIFIED`、production NOT_RUN、Released NO |
| Bulk Estimate change | Implemented | D-055 Approved。ordinary / Routine / mixed planned Entryへcommon positive / explicit `NULL` estimateを一つのatomic `BulkSetEntriesEstimateScoped` commandで適用し、Routineごとの`今回だけ` / `ルーティンに反映`、no-preselection、fill-all、個別override、confirm前no-write、success後selection retentionを実装。Occurrence override、Definition default / `defaults_revision`一回増分、eligible materialized propagation、explicit override / protected state保持、future no-materialization、placement revision不変、operation replayを含む。implementation `2eba5907455b92d301e57cc8b2eee087fa1aeebd`、APP migration `0014_bulk_estimate_scoped.sql`、local focused / full Worker / migration / typecheck / build、persistent nonprod backup gate / migration / deploy / integrityはPASS。browser authenticated feature mutation、remote exact replay、productionは`NOT_VERIFIED / NOT_RUN`、Released NO |
| Calendar | Planned | Domain / historyからのprojection。D-034のProjected RoutineOccurrence foundationを将来利用可能。具体Calendar UX / implementationは後続 |
| Timeline | Planned | planned / actual viewのprojection |
| Place | In design | planned meaningful destination foundation Approved |
| Execution Location | In design | optional Start / Complete LocationSnapshot foundation Approved |
| Map | Planned | Place / Location / Project historyからのprojection |
| Continuous location tracking | Planned | Separate future opt-in capability。initial Location scope外 |
| Obsidian integration | Planned | optional client |
| Legacy Vault importer | Planned | exact migration contractは未決 |

| Non-blocking ordinary Day mutation UX | Implemented | D-066 Approved。current-Day ordinary Task add、Task title / Project、Section / planned start、estimate、reorder、Start / Completeをmemory-only optimistic overlayとsingle serial client dispatcherで処理する。unsent intentのcoalesce、sent operationのexact retry identity、placement revision rebasing、scoped conflict barrier、provisional Add、transitive dependent execution queue、navigation defer / beforeunload guardを実装。deterministic prerequisite failureはfull queued descendant subtreeをcancelし、ambiguous rootはfull subtreeをexact retry成功まで保持する。Bulk、Routine scope、Project Board、日付移動、duplicate、destructive delete、manual execution correction、offline / persisted queueは対象外。local / persistent nonprod evidenceは`docs/CURRENT.md`のD-066 blockを正本とする |

| Completed Entry hard delete | Verified | D-067 Approved。current Dayのcompleted Entry一件だけを明示確認後にEntry + 全Executionごと削除する狭いcorrection exception。Task / Project / RoutineDefinition / RoutineOccurrence / snapshot / future Routine、他Entryは保持し、same-day Routine再生成を禁止する。planned deleteは既存BulkDeleteEntries、completed bulk / undo / restore / production mutationは対象外。APP `0020_delete_completed_entry.sql`はoperations CHECK-only compatibility migration。serial dispatcher corrective、local automated / migration-parent / persistent nonprod deploy、authenticated modal、承認済み使い捨て通常TaskのStart→Complete→完全削除→reload / fresh-tab、APP / AUTH integrityをPASS。controlled persistent destructive E2Eは2026-09-06にPASSし、production NOT_RUN、Released NO |

| Mode Management / Entry Mode v0.1 | Implemented / Verified | D-068 / D-070のowner-scoped Mode create / rename / reorder、current / established future ordinary planned Entryへのset / clear、Start snapshotを実装済み。D-071でSettings Mode Boardのheader、dense rows、52px action column、inline rename、midpoint D&D、focused row、`…` menu、notification、J/K/Arrow、`?` help、Escape / focus restorationをProject conventionへ揃え、local automated / persistent nonprod authenticated browser / read-only integrityをPASS。visible position、search、archive / delete、quick-create、Routine default / override、bulk変更、productionは対象外 |
| Future-Day Project assignment | Implemented / Verified | D-069 Approved。established future Dayのordinary planned Entryに限り、既存Projectのset / clearをTask-level metadataとして許可。future Task名、Section、planned start、estimate、Mode、Day、`placement_revision`は変更せず、preview / record-none / past / running / completed / Routine-derived / owner外を拒否し、archived Projectは新規選択不可・既存assignmentは表示可能とする。既存`UpdateTaskMetadata`、D-066 current serial dispatcher、future direct scoped retry / reconciliationを再利用し、新migration / new commandは追加していない。local Worker `5 / 5`、Web focused `3 / 3`、Web full `209 / 209`、persistent nonprod authenticated browser / DB integrityはPASS。production NOT_RUN、Released NO |
| Future-Day Mode assignment | Implemented | D-070 Approved。established future Dayのordinary planned Entryに限り、既存Modeのset / replace / clearをEntry-scopedとして実装。future preview、past、running、completed、Routine-derived、owner外はread-only / rejectとし、Mode relation、live rename、Start snapshot、move / duplicate、`placement_revision`をD-068どおり保持する。既存`SetEntryMode`、current D-066 dispatcher、future direct scoped retry / reconciliationを再利用し、migration / new command / API schemaは追加していない。unset optionを`Modeなし`からcanonical `—`へ変更し、current / future visible-label regressionを追加した。focused D-070 Web `3 / 3`、D-068 Mode Web `5 / 5`、Worker Mode `4 / 4`、full Worker `199 / 199`、full Web `213 / 213`、persistent nonprod deploy / safety / DB integrity / authenticated future browser set / replace / clear / fresh-tab persistenceはPASS。production NOT RUN、Released NO |
| Day keyboard S / I insertion | Implemented / Verified | D-074 Approved。current-Day ordinary planned Taskで`S`のStart / Completeをfocus復帰つきで実行し、pending Start後の2回目`S`を同じexecution identityのdependent Completeへqueueし、running A中のplanned BはD-073 InterruptEntryへ送る。`I`はTask直下またはSection scheduled area先頭のmemory-only draftを開き、server-authoritative `AddTaskToDay.placement`でsame Section / planned-start cohortへatomicに挿入する。Project / estimate / metadataは継承せず、N / append Addは不変。focused Web `4 / 4`、focused placement Worker `6 / 6`、full Web `4 files / 233 tests`、full Worker `24 files / 207 tests`、migration regression `4 scenarios`、persistent nonprod authenticated browser / reload / fresh-tab / APP-AUTH read-only integrityはPASS。migration / new command / dependency / productionは対象外。production NOT RUN、Released NO。 |
| Interrupt / Continuation v0.1 | Implemented / Verified | D-073 Approved。current logical Dayのordinary running Aからordinary planned BをStartすると、dedicated `InterruptEntry`でAを`interrupted`として履歴保持し、Bをactive化し、Aの同じTaskのplanned continuationをinterrupt minute / frozen Sectionへatomicに作成する。同一minuteはB直後、異なるminuteはinterrupt cohort末尾。異なるminuteの既存cohortに続くBはinsertion後もplanned startを保持し、continuationの後ろへ整合的に移動する。estimateはchain残量、Bの新規Task / Project / Mode snapshotを保存する。Routine / Quick Interrupt / non-current Day / auto-resumeは対象外。Worker `24 files / 206 tests`、focused placement `6 / 6`、Web `4 files / 229 tests`、focused D-073 Web `3 / 3`、migration `4 scenarios`、persistent nonprod authenticated browser、APP/AUTH read-only integrityをPASS。production NOT RUN、Released NO |
| Day fixed header and task-list scroll | Implemented / Verified | D-075 Approved。Day date/navigationとtoolbarをfixed region、table column headerをopaque sticky region、Section summary / Task rowをsingle `.day-surface` scroll ownerへ分離し、row overflow menuをbody portalでclip回避した。Project / Mode / column customization、keyboard focus / overlay、D-074 S / I、API / Domain / schema / migration / dependencyは不変。implementation `8f7d00c`、focused Web `2 files / 211 tests`、full Web `4 files / 236 tests`、full Worker `24 files / 207 tests`、typecheck / build / exact nonprod build / Wrangler dry-run、persistent nonprod authenticated browser、APP/AUTH read-only integrityはPASS。viewport resize gestureはcurrent browser connectorにresize APIがないためNOT_RUN、production NOT_RUN、Released NO |
| D-075 fixed chrome corrective / Future-Day I parity | Implemented / Verified | D-075 correctiveは`.day-header` / `.day-toolbar`のnon-shrink、`.day-surface`の`flex: 1 1 0`、shell bottom reservation除去、Runner visible時だけの88px trailing escapeを実装。D-076 Approvedによりestablished future Dayのordinary planned Task / Sectionへ`I`を拡張し、既存AddTaskToDay placement contractをlogical date + established Day identity guard付きで再利用。implementation `ba18d72`、routing corrective `c9f2fe7`、focused Web `2 files / 214 tests`、full Web `4 files / 240 tests`、focused Worker `16 / 16`、full Worker `24 files / 207 tests`、migration regression `4 scenarios`、typecheck / exact nonprod build / dry-runはPASS。persistent nonprod Worker `a6f1a16f-4835-4a27-ac89-bcc055fb3303`、short / long geometry、Runner overlap / last-row escape、future Task / Section `I`、same-tab reload / fresh authenticated tab、APP/AUTH read-only evidence、console `0 / 0`をPASS。viewport resize gesture、production、restore、cleanup、ReleasedはNOT_RUN / NO |
| D-077 Instant Day interaction v0.1 | Implemented / Verified | Current-Day ordinary planned Taskのtitle / Project / Mode / Section / estimate / planned startを、D-066のmemory-only effective overlayとglobal serial dispatcherで即時受理し、同一fieldのunsent coalesce、title / Projectのfield-level metadata merge、planning intent→Start依存、reconcile時のno-rewind、provisional Add / lifecycle / retry / ambiguity境界を維持。`保存中 n件`は論理的な未解決intent数を表示し、cross-Task編集を保存中でも阻害しない。D&D / reorder、future / past / Routine、offline、persistent queue、API / schema / migration / dependencyはscope外。実装 `e9d644abf5e9a9d057c58fe77c70936e954c502c`、focused Web `App.test.tsx 212 / 212`、full Web `4 files / 244 tests`、full Worker `24 files / 207 tests`、typecheck / build / exact nonprod build / Wrangler dry-run / diff-check、persistent nonprod authenticated browser、reload / fresh tab、APP/AUTH read-only integrity、console `0 / 0`をPASS。production NOT RUN、Released NO |
| D-078 Non-blocking repeated same-Section reorder v0.1 | Implemented / Verified | Current established Dayのsame-Section / same-planned-start cohortに限り、pointer D&Dと`Shift + ArrowUp / ArrowDown`をeffective pending orderから即時受理。sent Reorderはimmutable、unsent same-segment intentだけをbarrier-awareにcoalesceし、unsent no-op、failure/conflict cancellation、ambiguous exact retry、D-077 save countを保持。実装 `7bdfc4be82439f8518672e0bc120f374c435a5e6`、focused D-078 Web `6 / 6`、full Web `4 files / 249 tests`、relevant Worker `4 files / 38 tests`、full Worker `24 files / 207 tests`、typecheck / build / exact nonprod build / Wrangler dry-run / diff-checkをPASS。persistent authenticated browserはnormal-width `1280 × 720` / DPR 1 / sidebar closedで実pointer D&D、同一cohort内の連続drag、effective order、`保存中 2件`、focus維持、same-tab reload / fresh authenticated tab persistence、console `0 / 0`をPASS。Runner4 visibleでもP1→P2の実pointer reorder、`保存中 1件`、trailing escape、running boundary rejectionをPASS。Worker version `500e93af-0308-45b5-84da-627eb44eeaab`、APP/AUTH read-only integrity PASS、migration helperはNOT_RUN、production NOT RUN、Released NO |
| D-079 Non-blocking cross-Section Move v0.1 | Implemented / Integrated | Current established Dayのordinary planned Entryに限り、既存MoveEntry commandを再利用してpointer/selectorのcross-Section Moveをeffective row membershipへ即時反映。sent operation immutable、same-Entry unsent tailだけをbarrier-awareにcoalesceし、different-Entry order、Move→Start、failure/conflict/ambiguity、Sectionなし NULL start、frozen Section start、D-078 reorder/planned-start barrierを維持。implementation `7d788b34f7a4986ef92344956a9dd1ed68546918`、full Web `253`、full Worker `207`、typecheck/build/nonprod dry-run PASS、Worker `4963f000-68a3-45fc-95dc-fc7eafbba582`、selector browser / Runner / persistence / DB PASS。actual pointer drag was attempted but not accepted by the browser connector and remains `NOT_VERIFIED`; production NOT RUN、Released NO |
| D-080 Provisional Add → I keyboard chaining v0.1 | Implemented / Verified | Current established Dayのprovisional Add rowをstable Entry anchorとして、先行Addのresponse待ちなしに`I`でchild Addを連鎖。親子依存、recursive effective placement、sent exact identity、failure/conflict/ambiguity、focus/no-rewind、provisional `S` no-writeを保持。implementation `2ae90cce106e6c359954f67f7606bdba94a67294`、focused Web `2 / 2`、full Web `4 files / 255 tests`、full Worker `24 files / 207 tests`、migration regression `4 scenarios`、typecheck/build/exact nonprod build/Wrangler dry-run/diff-check PASS。Worker `2c13f640-a31c-4680-bb39-fe5e36091c68`、authenticated browser A/B/C/D chain immediate + same-tab/fresh-tab persistence、provisional `S` no-write、console `0 / 0`、APP/AUTH read-only integrity PASS。natural browser race timing was not observable; deferred automated evidence remains authoritative。API / schema / migration / dependency unchanged、production NOT RUN、Released NO |
| D-080 corrective — immediate post-Enter focus handoff | Implemented / Verified | Product Owner報告で判明した`Enter`後の`照合中`待ちを、`commitDraft()`時点のstable provisional row focus handoffで補正。deferred unresolved Add regressionで、manual focusなしに`保存中…` rowへfocus後、親response前の`I` child draftをPASS。corrective `98c9c39ac4f1ca7303b1c71c7c3d6f37f05f34f8`、focused provisional Add `4 / 4`、full Web `256`、full Worker `207`、typecheck/build/exact nonprod build/Wrangler dry-run/diff-check PASS。Worker `cc940824-5d8e-4c90-88f8-20347fe0dff5`、persistent pending-row observation、same/fresh-tab persistence、console `0 / 0`、APP/AUTH integrity PASS。自然なbrowser race timingはNOT_OBSERVED、migration helperはNOT_RUN、API / Worker / schema / migration / dependency / security unchanged、production NOT RUN、Released NO |
| D-081 Actual-Section Start + execution-first Day Table | Implemented / Verified | D-043 planned synchronizationを保ったまま、Workerがfrozen Day Section contextからactual Start Sectionを解決し、running / completedのSectionをactual Sectionへ変更、planned startを保持。Day projectionはhistorical actual-start groupをplanned groupより前に表示し、Reorderはhistorical physical positionsを変更せずplanned cohort slotsだけを再利用。Interrupt target / continuationも整合。focused Worker `4 files / 44 tests`、full Worker `24 files / 210 tests`、full Web `4 files / 256 tests`、typecheck / build / exact nonprod build / dry-run / diff-check、persistent nonprod Start/Complete/Interrupt browser、reload / fresh tab、APP/AUTH read-only evidence、console `0 / 0` PASS。Worker `8ff2080f-afe6-48b9-a5ac-fdfba05d6a45`、migration `NOT_REQUIRED`、dependency/API unchanged、real-local browserおよびpointer D&D / Shift reorder browser実測はNOT_RUN、production NOT RUN、Released NO |
| D-082 Auto-carry overdue planned Tasks to current Section | Implemented / Tested | Current established Dayの設定ON時だけ、Serverがfrozen Section intervalからcurrent Sectionを解決し、過去timed Sectionの未実行planned ordinary / Routine-derived Entryをcurrent Section logical startへatomicにcarryする。ordinary relative order、target cohort後置、Routine override、D-081 historical position、placement revision、operation checkpoint / exact replay / CAS / ambiguityを保持。Settings UIはowner-scoped authenticated APIと15秒 boundary reconcileを使用し、past / future / preview / Sectionなし / lifecycle / suppressed Routineは対象外。APP migration `0024_auto_carry_overdue_planned.sql`、AUTH unchanged、focused Worker `2 / 2`、full Worker `25 files / 212 tests`、full Web `4 files / 256 tests`、typecheck/build/nonprod dry-run PASS。persistent nonprod browser/DB、remote migration helperはNOT_RUN、production NOT RUN、Released NO |
